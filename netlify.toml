# Netlify configuration

[build]
  publish = "dist"
  command = "npm run build"

# Enable Netlify Identity for CMS
[build.environment]
  NODE_VERSION = "18"

# SPA redirects
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
